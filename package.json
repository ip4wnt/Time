{
  "name": "chronum",
  "version": "2.0.0",
  "description": "ХРОНУМ — личный трекер времени: занятия, задачи, еда, мысли, счётчики, заметки",
  "private": true,
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "migrate": "node -e \"require('./server/db').migrate().then(()=>{console.log('ok');process.exit(0)})\"",
    "import": "node scripts/import-legacy.js",
    "test:isolation": "node scripts/test-isolation.js",
    "check:scoping": "node scripts/check-scoping.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.13.0"
  }
}
